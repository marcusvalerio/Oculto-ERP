window.XmlEngine={
  esc(v=''){return String(v).replace(/[<>&'\"]/g,c=>({'<':'&lt;','>':'&gt;','&':'&amp;',"'":'&apos;','\"':'&quot;'}[c]))},
  generate(doc){const e=this.esc.bind(this);const type=doc.type==='NFE'?'NFe':doc.type==='CTE'?'CTe':'MDFe';const model=doc.type==='NFE'?'55':doc.type==='CTE'?'57':'58';return `<?xml version="1.0" encoding="UTF-8"?>\n<DocumentoSimulado xmlns="urn:fiscal-generator:estudo" tipo="${type}" modelo="${model}" validadeFiscal="false">\n  <Identificacao>\n    <Numero>${e(doc.number)}</Numero>\n    <Serie>${e(doc.series)}</Serie>\n    <Chave>${e(doc.key||'')}</Chave>\n    <Ambiente>SIMULACAO_EDUCACIONAL</Ambiente>\n  </Identificacao>\n  <Emitente><RazaoSocial>${e(doc.issuer)}</RazaoSocial><CNPJ>${e(doc.cnpj)}</CNPJ></Emitente>\n  ${doc.recipient?`<Destinatario><RazaoSocial>${e(doc.recipient)}</RazaoSocial><Documento>${e(doc.recipientDoc||'')}</Documento></Destinatario>`:''}\n  <Valores><Quantidade>${e(doc.qty||1)}</Quantidade><ValorUnitario>${e(doc.unit||0)}</ValorUnitario><ValorTotal>${e((Number(doc.qty||1)*Number(doc.unit||0)).toFixed(2))}</ValorTotal></Valores>\n  <Aviso>DOCUMENTO SIMULADO — SEM VALIDADE FISCAL — NAO TRANSMITIDO A SEFAZ</Aviso>\n</DocumentoSimulado>`}
};
