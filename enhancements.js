/* Compatibility layer intentionally kept empty.
   The application flow is now implemented in app.js as a single source of truth. */
(function(){ window.FiscalEnhancements={version:'unified'}; })();
